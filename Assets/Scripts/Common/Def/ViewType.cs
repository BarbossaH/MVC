

    //the type of all views, the name must be same with script filenames
    namespace Common.Def
    {
        public enum ViewType
        {
            StartView,
            SettingsView,
            MessageView,
            LoadingView,
            SelectLevelView,
            BattleSelectHeroView,
            BattleView,
            DragHeroView,
        }
    }
